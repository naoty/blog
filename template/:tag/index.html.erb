<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>blog.naoty.dev #<%= tag %></title>
    <meta name="description" content="All posts tagged with #<%= tag %>">
    <meta property="og:title" content="blog.naoty.dev #<%= tag %>">
    <meta property="og:description" content="All posts tagged with #<%= tag %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://blog.naoty.dev/<%= tag %>/">
    <meta property="og:image" content="https://blog.naoty.dev/icon-512.png">
    <meta name="twitter:card" content="summary">
    <meta name="twitter:creator" content="@naoty_k">
    <link href="/feed.xml" rel="alternate" type="application/atom+xml">
    <link href="/normalize.css" rel="stylesheet">
    <link href="/main.css" rel="stylesheet">
    <link href="/favicon.png" rel="shortcut icon" type="image/png">
  </head>
  <body>
    <main>
      <h1 class="title mt-0">#<%= tag %></h1>
      <nav>
        <ul class="mb-0">
        <%- posts.each do |post| -%>
          <li class="list-item">
            <time datetime="<%= post.time.strftime('%Y-%m-%dT%H:%M:%S.%3N%z') %>"><%= post.time.strftime('%Y-%m-%d') %></time>
            <a href="/<%= post.id %>/"><%= post.title %></a>
          <%- post.tags.each do |tag| -%>
            <a href="/<%= tag %>/">#<%= tag %></a>
          <%- end -%>
          </li>
        <%- end -%>
        </ul>
      </nav>
      <footer>
        <nav>
          <ul class="footer-links mb-0">
            <li class="footer-link"><a href="https://naoty.dev">Home</a></li>
            <li class="footer-link"><a href="/">Posts</a></li>
          </ul>
        </nav>
      </footer>
    </main>
  </body>
</html>
